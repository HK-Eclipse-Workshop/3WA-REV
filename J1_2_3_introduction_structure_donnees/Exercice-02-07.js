// Dans le code que vous avez donné, myFunc est une fonction de type "expression de fonction" (ou fonction anonyme) qui est définie après son évaluation. 
// Elle contient également une fonction interne bar, qui est également une expression de fonction.
// En JavaScript, les fonctions déclarées (aussi appelées "définitions de fonction") sont définies dès le début de leur portée (le script ou la fonction qui les contient). 
// En revanche, les expressions de fonction (ou fonctions anonymes) sont définies après leur évaluation, 
// c'est-à-dire qu'elles ne sont accessibles qu'une fois que le code qui les définit est atteint lors de l'exécution.